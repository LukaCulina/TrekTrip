spring.application.name=trektrip

server.port=${PORT:8080}
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-autumn-tree-a2j9wgrp-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_l78MVjwzEmTR}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

spring.h2.console.enabled=false

spring.jpa.defer-datasource-initialization=true

# upload.path=./frontend/public/uploads

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Connection pool
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5

# Logging (debug for troubleshooting, info for production)
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO



